     
        <div class="giftboxinnerblock">
          <input type="checkbox" name="giftboxdesign" id="giftboxdesign" otherid="5765638881331" class="radioBtnClass" aria-invalid="false">
          <label for="giftboxdesign" class="giftboxkabelclass1">Fall and Pico - ₹200</label>
        </div>

        <script>
         document.addEventListener('DOMContentLoaded', function() {
    var giftboxInputs = document.querySelectorAll("input[name='giftboxdesign']");

    giftboxInputs.forEach(function(input) {
        input.addEventListener('click', function() {
            var checkedInput = document.querySelector("input[name='giftboxdesign']:checked");
            if (checkedInput) {
                var giftboxidother = parseInt(checkedInput.getAttribute('otherid'));
                var giftboxname = checkedInput.value;

                // Check if the ID is valid before making the POST request
                if (!isNaN(giftboxidother)) {
                    var xhr = new XMLHttpRequest();
                    xhr.open('POST', '/cart/add.js', true);
                    xhr.setRequestHeader('Content-Type', 'application/json;charset=UTF-8');
                    xhr.onreadystatechange = function() {
                        if (xhr.readyState === XMLHttpRequest.DONE) {
                            if (xhr.status === 200) {
                                console.log('Item added to cart:', xhr.responseText);
                            } else {
                                console.error('Error adding item to cart:', xhr.status, xhr.statusText);
                            }
                        }
                    };
                    var data = JSON.stringify({
                        items: [{ quantity: 1, id: giftboxidother }]
                    });
                    xhr.send(data);
                } else {
                    console.error('Invalid gift box ID:', giftboxidother);
                }
            }
        });
    });
});


        </script>